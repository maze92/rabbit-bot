// GameNews module extension for OzarkDashboard
// Aqui no futuro vamos isolar a lógica da tab GameNews.

(function () {
  if (!window.OzarkDashboard) return;

  const D = window.OzarkDashboard;

  // Exemplo de extensão futura:
  // const originalLoadGameNews = D.loadGameNews;
  // D.loadGameNews = async function () { return originalLoadGameNews(); };
})();
