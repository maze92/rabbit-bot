// Moderation module extension for OzarkDashboard
// Aqui no futuro podemos mover a lógica de Logs & Cases para este ficheiro.

(function () {
  if (!window.OzarkDashboard) return;

  const D = window.OzarkDashboard;

  // Exemplo de extensão futura:
  // const originalLoadLogs = D.loadLogs;
  // D.loadLogs = async function () { return originalLoadLogs(); };

  // const originalLoadCases = D.loadCases;
  // D.loadCases = async function () { return originalLoadCases(); };
})();
