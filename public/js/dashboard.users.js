// Users module extension for OzarkDashboard
// For now, this file just attaches to the global namespace without changing behaviour.
// In próximos passos, a lógica específica da tab de utilizadores pode ser movida para aqui.

(function () {
  if (!window.OzarkDashboard) return;

  const D = window.OzarkDashboard;

  // Exemplo de ponto de extensão futuro:
  // D.loadUsers = D.loadUsers || function () { ... };
  // D.loadUserHistory = D.loadUserHistory || function (user) { ... };

})();
